---
import { getCollection } from "astro:content";
import DetailLayout from "@/layouts/DetailLayout.astro";
import StubContent from "@/components/StubContent.astro";

const locale = "el" as const;

export async function getStaticPaths() {
  const entries = await getCollection("departments");
  return entries.map((entry) => ({
    params: { slug: entry.id },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const pathname = Astro.url.pathname;
---

<DetailLayout
  title={`${entry.data.name[locale]} — HBOT Science`}
  description={entry.data.shortDesc[locale]}
  locale={locale}
  pathname={pathname}
  pageHeading={entry.data.name[locale]}
  eyebrow="Τμήμα"
  noindex={true}
>
  <p class="text-lg text-slate-700 leading-relaxed">{entry.data.shortDesc[locale]}</p>
  <StubContent phase="3.D" locale={locale} />
</DetailLayout>
