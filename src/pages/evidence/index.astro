---
import IndexLayout from "@/layouts/IndexLayout.astro";
import StubContent from "@/components/StubContent.astro";
import { t } from "@/i18n";

const pathname = Astro.url.pathname;
const locale = "en" as const;
---

<IndexLayout
  title={`${t("evidence.section.title", locale)} — HBOT Science`}
  description={t("evidence.section.subtitle", locale)}
  locale={locale}
  pathname={pathname}
  badge={t("evidence.section.badge", locale)}
  pageHeading={t("evidence.section.title", locale)}
  pageSubtitle={t("evidence.section.subtitle", locale)}
  badgeColor="violet"
  noindex={true}
>
  <StubContent phase="3.C" locale={locale} />
</IndexLayout>
