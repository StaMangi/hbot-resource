# HBOT Resource — Project Status

**Working dir:** `/Users/stamoulismanginas/Projects/hbot-resource/`
**New repo:** https://github.com/StaMangi/hbot-resource (public)
**Forked from:** [StaMangi/hbot_clinical_resource](https://github.com/StaMangi/hbot_clinical_resource) — preserved at `_henry-dunant-source/`
**Live source site:** https://hbotresource-wcjp4x3h.manus.space
**Target hosting:** Cloudflare Pages
**Target domain:** `hbotscience.org` (registered with Namecheap, DNS pointing to Cloudflare nameservers `ken.ns.cloudflare.com` / `nena.ns.cloudflare.com`, awaiting activation)

## Current phase
**LIVE.** Phase 6 launch closed on 2026-04-29. Site is live at **https://hbotscience.org** (apex). Lighthouse mobile production: Performance 100 / Accessibility 100 / Best Practices 100 / SEO 100 across 5 representative pages; LCP 0.12–0.24s. All 41 sampled production routes return 200; 114 URLs in the sitemap, all canonical to apex; sitemap submitted to Google Search Console + Bing Webmaster Tools; quadruple-grep clean for legacy strings; BreadcrumbList JSON-LD validated EN+EL on indication detail pages. **Multilingual expansion (DE/IT/AR) is a separate post-launch phase per Stamos's directive — not started without explicit instruction.**

**Live preview:** https://hbot-resource.pages.dev (HTTPS, `noindex` during build phase — lifts at Phase 6 launch). **115 routes** deployed. Indications taxonomy: FDA-Approved Indications (14) + Emerging Evidence (5 — Long COVID, Fibromyalgia, Inflammatory Dermatoses, Aesthetic & Reconstructive Recovery, Endometriosis). 19 indications total. **44 references** in collection, all CrossRef-verified or recognised non-DOI repository (PMC, NCBI Bookshelf, UHMS, scivisionpub, clinicaltrials.gov). **All 12 departments populated** (3 new in Phase 4: dermatology, plastic-surgery, women's-health). DOI verifier wired into Cloudflare build via `prebuild` hook. **Greek terminology register locked at `docs/research/el-glossary.md`** — full Phase 4 register, ~120 EN→EL term pairs (study types, evidence levels, caveat register, symptom vocabulary, treatment/protocol, trial names, plus per-cluster additions § 4.C/4.D/4.E/4.F). Protocol vocabulary fully bilingual via 4.D schema migration. Three standalone pages (`/safety/`, `/programme-design/`, `/operator-blueprint/`) populated bilingual EN+EL. **Cumulative Phase 4 editorial catches: 12** (6 inherited integrity issues from 4.B · 0 from 4.C · 1 plan-vs-PDF mismatch in 4.D · 2 EL parity fixes in 4.D · 1 plan-vs-PDF mismatch in 4.E · 1 unsupported pilot-study claim caught in 4.F · 1 missing safety-claim citation caught in 4.G). Lighthouse mobile (Brave headless) on 4 representative pages at PR #5 final commit: Performance 98–100 / Accessibility 100 / Best Practices 100 / SEO 66 (SEO 66 is the `noindex` penalty — lifts at launch).

## Phase 1 (audit) — done
- Inventory + memory seeded · Phase 2 plan written · Lighthouse baseline (Desktop 88/82/81/92, Mobile 59/75/82/92) · Content extracted to `data-export/` (66 entries + 286 i18n keys).

## Phase 2.A (bootstrap) — done (this session)
- Henry Dunant source moved to `_henry-dunant-source/` (read-only reference, included in repo for provenance)
- Local `.git/` from the original clone dropped; fresh git history initialised
- Astro 5.18.1 project bootstrapped at repo root with: React 19, Tailwind v4 (via `@tailwindcss/vite`), `@astrojs/sitemap`, `@astrojs/mdx`, `@astrojs/check`
- Folder skeleton: `src/{pages,layouts,components,lib,styles,i18n}` + `public/`
- **`BaseHead.astro` SEO scaffolding wired from day one** (per Stamos addition 1):
  - Canonical URL, hreflang en/el/x-default, Open Graph, Twitter Card, MedicalWebPage JSON-LD, robots noindex, favicon, font preconnects.
- Initial pages: `/` (EN placeholder), `/el/` (EL mirror), `/404` — all build to static HTML.
- `noindex` set on every page during preview phase.
- Sitemap auto-generated with EN ↔ EL `xhtml:link` alternates.
- `robots.txt` allows all crawlers and points to `sitemap-index.xml`.
- Build verified: `pnpm build` produces 3 pages + sitemap in ~1 second.
- GitHub repo created: https://github.com/StaMangi/hbot-resource (public)
- Initial commit pushed to `main`. CHANGELOG attributes Henry Dunant source.

## Phase 2.B (content migration) — done (this session)
- Domain placeholder fixed (`hbotresource.com` → `hbotscience.org`) before content migration.
- `src/content.config.ts` defines Zod schemas for 9 collections with bilingual EN/EL fields.
- `scripts/migrate-content.mjs` reads `data-export/hbot-content.json`, applies de-branding rewrites, generates per-entry YAML files.
- **80 entries written** across 9 collections:
  - mechanisms (5) · indications (14) · departments (9) · departments-without-hbot (6) · longevity (6) · research-studies (8) · strategic-recommendations (4) · references (24) · site-stats (4)
- De-branding pass rewrote one entry (`strategic-recommendations/research`) to drop "Henry Dunant Hospital" and "Eastern Mediterranean region" — both EN and EL.
- Verification: zero "Henry Dunant" or "Νοσοκομείο Henry" matches anywhere in `src/content/`.
- Build clean: Astro loads + validates all 80 entries against Zod schemas, ~1 second.

## Phase 2.C (i18n + base shell) — done (this session)
- `scripts/migrate-i18n.mjs` — second one-shot migration script. Reads `data-export/hbot-i18n.json`, applies approved de-brand replacements at port-time, emits `src/i18n/{en,el}.ts`.
- **De-brand approved replacements applied:** 8 EN replacements + 7 EL replacements + 2 deletions per language (`nav.subtitle`, `strategy.market.stat2`).
- **Stealth catch:** EL `nav.subtitle` was `"Νοσοκομείο Ερυθρός Σταυρός"` (Red Cross Hospital — the Greek localizers' equivalent for Henry Dunant Hospital, which is run by the Hellenic Red Cross Society). Plain regex on "Henry Dunant" / "Ντυνάν" would have missed it. Pattern set now permanently includes "Ερυθρός Σταυρός".
- **i18n keys:** 134 EN + 148 EL (was 136 / 150 before deletions).
- `src/i18n/index.ts` — `t(key, locale)` helper, EN-canonical with locale fallback to EN then to the key itself.
- `src/lib/seo.ts` — added `localePath()` helper for path-only locale switching.
- `SITE_NAME` updated to `"HBOT Science"` for consistency with `nav.brand`.
- `src/components/Nav.astro` — brand link + language toggle that preserves the current path. Zero JS, accessible (`hreflang` + `aria-label`).
- `src/components/Footer.astro` — editorial publisher line *"Editorial publisher: IN2050 Ltd · Cyprus · Reg. HE416406"*, editorial-policy link, language toggle, copyright. No IN2050 logo, no proprietary badge, no Henry Dunant.
- `/`, `/el/`, `/404` all wired through `Nav` and `Footer` via `BaseLayout` slots.
- **Quadruple grep verification clean** in shippable code (`src/`, `dist/`, `public/`): zero matches for `Henry Dunant`, `Νοσοκομείο Henry`, `Ντυνάν`, `Ερυθρός Σταυρός`. Documentation files (CHANGELOG/README/STATUS/PHASE-2-PLAN), `data-export/*` (canonical pre-de-brand source), `scripts/migrate-*.mjs` (regex patterns), and Lighthouse baselines retain Henry Dunant references — all expected and correct.
- Build clean: 3 pages + sitemap, ~1 s.

## Phase 3 audit items (deferred from earlier phases)
- **EN/EL asymmetry:** EL has 14 longevity strings (`longevity.landmark.*`, `longevity.stat.*`, `longevity.wellness.athletic.*`, `longevity.wellness.cognitive.*`) not present in EN. Inherited from the Henry Dunant codebase. Decide in Phase 3: (a) backfill EN translations, or (b) remove dead EL keys.
- **Missing components for `strategy.market.stat2`:** key was deleted in 2.C. Strategy section component (Phase 2.D) must rebuild this stat block thoughtfully — placeholder text would have rotted.

## Phase 5 polish items
- **Mobile language-toggle redundancy.** Nav and Footer both expose language toggles. On mobile narrow viewports this creates duplicated friction. Decide: hide Nav toggle on mobile (keep Footer), hide Footer toggle on mobile (keep Nav), or accept both. Phase 5 polish.

## Phase 2.D (section content rendering) — done (this session)
- Added `astro-icon` + `@iconify-json/lucide` for static lucide icons (tree-shaken at build).
- Reusable subcomponents: `SectionHeader.astro`, `ProtocolPanel.astro`, `RefTags.astro`.
- 9 section components in `src/components/sections/` — Hero, Mechanisms, FDA, Departments, NoHBOT, Longevity, Evidence, Strategy, References. All zero-JS, all read from content collections via `getCollection()`.
- Long-scroll homepage composed in `src/components/HomePage.astro`, used by `/` (EN) and `/el/`.
- **`strategy.market.stat2` rebuild:** chose `"258+ Peer-reviewed citations"` (English) / `"258+ Αξιολογημένες αναφορές"` (Greek). Reason: matches the broader 258-citation claim already in `evidence.summary.body` and signals broad evidence base, which is the right market-positioning frame between clinical and wellness.
- New i18n keys added: `strategy.market.stat2.val`, `strategy.market.stat2` (both languages). `migrate-i18n.mjs` updated with an `ADDITIONS` map so re-running stays idempotent.
- **EN/EL asymmetry handling:** Longevity section's landmark study banner + wellness callouts only render in EL (where the keys exist). EN gets the main longevity grid only. Added `has(key, locale)` to `src/i18n/index.ts` so sections can conditionally render locale-only blocks.
- **Content rendered correctly** in built output: 5 mechanisms · 14 FDA cards · 9 departments (with their applications) · 6 dept-without-hbot · 6 longevity · 8 research studies · 4 strategic recommendations · 24 references with `id="ref-N"` anchors.
- **Quadruple grep clean** in shippable code (`src/`, `dist/`, `public/`).
- **Page weight (gzipped):** 26.9 KB EN, 33.4 KB EL — far under 500 KB target. Zero `<script>` tags reference real JS (only the inline JSON-LD block ships).
- **Lighthouse local run blocked:** Brave headless consistently rejects HTTP localhost with `CHROME_INTERSTITIAL_ERROR` (Brave's Shields/HTTPS-only behaviour, not overrideable via flags). Tried multiple flag combinations including `--unsafely-treat-insecure-origin-as-secure`, fresh user-data-dirs, disabled Brave features. Formal Lighthouse measurement is **deferred to 2.F** when Cloudflare Pages serves over HTTPS (proven working in Phase 1 baselines).
  - Architecture-derived expectations against the Phase 1 baseline (mobile 59 perf): zero-JS static rendering should deliver ≥95 across all four pillars on both mobile and desktop. Verified in 2.F.

## Phase 2.F (Pages preview + Lighthouse) — done (this session)
- Cloudflare Pages connected to `StaMangi/hbot-resource` (Stamos via dashboard). Preview URL: https://hbot-resource.pages.dev. Build settings: Astro preset, `pnpm build`, `dist/` output, `NODE_VERSION=22`.
- First deploy succeeded. Auto-deploys on every push to `main`.
- Lighthouse mobile + desktop run against the preview URL. Reports saved to `docs/2d-lighthouse-{mobile,desktop}.report.{html,json}` and human-readable summary to `docs/2d-lighthouse-summary.md`.

### Scores (preview URL, HTTPS)
| Pillar | Mobile (was) | Desktop (was) | Target |
|---|---|---|---|
| Performance | **81** (59) | **96** (88) | ≥95 |
| Accessibility | **95** (75) | **95** (82) | ≥95 |
| Best Practices | **100** (82) | **100** (81) | ≥95 |
| SEO | **66** (92) | **66** (92) | ≥95 |

- SEO drop is **artificial** — only failing audit is `is-crawlable`, caused by the deliberate `noindex` during preview. Will return to ≥95 when domain is attached and `noindex` flips off at Phase 6 launch.
- **Mobile Performance 81 is below target.** Diagnosis: render-blocking external stylesheets (Google Fonts ~992ms + own _astro CSS ~450ms). Page weight only 110 KiB total; zero JS; zero unused CSS. The bottleneck is the font CDN, not the architecture. Four fix-path options written up in summary doc.

## Phase 3.A (foundation) — done (this session)
### 3.A.1 — Font fix (deferred Option C from 2.F)
- `@fontsource/montserrat` (4 weights) + `@fontsource/lato` (3 weights), Latin + Latin-Ext subsets, total 14 woff2 files / 312 KB. Browser fetches only 3-4 based on weight + unicode-range.
- Greek subsets not available in fontsource for either typeface (same as the original site — Lato never had Greek; Montserrat had it on Google CDN but fontsource omits it). Greek text on `/el/` falls back to system Greek fonts cleanly. Phase 5 polish can self-host Noto Sans Greek if visual fidelity on Greek pages becomes a concern.
- `build.inlineStylesheets: 'always'` in `astro.config.ts` — all CSS inlined into per-page `<style>` block; eliminated the 2.F-flagged 450 ms render-blocking external CSS.
- Google Fonts CDN `<link>` + preconnects removed from `BaseHead.astro`. Confirmed zero references to fonts.googleapis.com / fonts.gstatic.com in any built page.

### 3.A.3 — Three reusable layouts
- `src/layouts/LandingLayout.astro` — tier 1 + 2 (homepage, clinical, wellness). Hero slot + main slot.
- `src/layouts/IndexLayout.astro` — collection index pages with built-in SectionHeader + breadcrumbs slot.
- `src/layouts/DetailLayout.astro` — per-entry pages with two-column desktop (main + aside), single-column mobile, breadcrumbs/eyebrow/h1/main/aside slots.

### 3.A.2 — URL stubs (30 new files, 76 routes generated)
- 6 static landing stubs: `/clinical/`, `/wellness/`, `/about/` × EN + EL.
- 16 index stubs: `/mechanisms/`, `/indications/`, `/departments/`, `/departments/without-hbot/`, `/longevity/`, `/evidence/`, `/references/`, `/protocols/` × EN + EL.
- 8 dynamic `[slug].astro` stubs (4 collections × 2 locales) generating 68 detail routes.
- Each stub renders `StubContent` placeholder labelled with its expected phase (3.B / 3.C / 3.D / 6).
- All `noindex={true}` during preview.

### Existing 2.D pages preserved (per Stamos amendment B)
- `/`, `/el/`, `/404` continue to render the long-scroll homepage as fallback.
- `src/components/sections/*.astro` and `src/components/HomePage.astro` stay in place; deletion happens at end of 3.F after card primitives are extracted.

### Lighthouse — pass criterion met
| Pillar | Mobile | Desktop | Target |
|---|---|---|---|
| Performance | **98** (was 81) | **100** (was 96) | ≥95 ✅ |
| Accessibility | 95 | 95 | ≥95 ✅ |
| Best Practices | 100 | 100 | ≥95 ✅ |
| SEO | 66 | 66 | (deliberate noindex; flips at Phase 6) |

Mobile FCP / LCP halved: 3.6 s → 1.8 s. Reports in `docs/3a-lighthouse-{mobile,desktop}.report.{html,json}` + `docs/3a-lighthouse-summary.md`.

### Build verification
- `astro check`: 0 errors / 0 warnings / 0 hints across 60 files.
- `pnpm build`: 93 routes built in 1.43 s.
- Sitemap: 92 routes (404 excluded), EN/EL alternates wired.
- Quadruple-grep clean in `src/`, `dist/`, `public/`.

## Phase 3.B — done (this session)

### i18n parity achieved (150 EN / 150 EL keys)
- 14 EL-only longevity strings backfilled into EN with British English spelling. `migrate-i18n.mjs` updated with `EN_LONGEVITY_BACKFILL` map; re-run produces matching output.
- Stamos approved 12 verbatim and amended 2:
  - **Flag #1**: `longevity.wellness.cognitive.tag` → "Wellness Programme" (EN) and "Πρόγραμμα Ευεξίας" (EL). Drops "Premium" framing for editorial-credibility tone.
  - **Flag #2**: `longevity.wellness.athletic.desc` — drops the unsourced "30-50%" figure in BOTH languages. Qualitative claim retained (supported by ref-22 / Fife 2022 meta-analysis). **Editorial-integrity precedent established**: every claim citable, no exceptions, no asymmetry between languages.
- Quadruple-grep verification still clean.

### Nav redesign (zero JS, accessible)
- Brand · Clinical / Wellness · | · Mechanisms / Indications / Departments / Longevity / Evidence / References · language toggle.
- Active-link highlighting via build-time pathname comparison.
- Sticky position so nav stays visible on deep pages.
- Mobile menu: native `<details>`/`<summary>` — hamburger opens a flat list, dismissible, accessible. No JS.

### Tier 1 — new homepage (replaces long-scroll)
- 3-screen short home: hero with 4 stats · two soft-routing path cards (Clinical, Wellness) · 4-tile library preview.
- New components: `HomeStat.astro`, `HomePathCard.astro`, `HomeLibraryTile.astro`.
- Old `HomePage.astro` and `src/components/sections/*.astro` stay in src/ (per amendment B) — code-level fallback until 3.F deletion.

### Tier 2 — /clinical/ and /wellness/ landings (replaces 3.A stubs)
- Both EN + EL.
- Clinical: hero · 9-department grid (cards link to `/departments/<slug>/` with FDA/research counts from collection) · 4-tile reference-material section · mailto CTA.
- Wellness: hero · 6-application grid (cards link to `/longevity/<slug>/` with stat + summary) · 4-tile operator-architecture section · mailto CTA.
- Both use `mailto:info@hbotscience.org` with prefilled subject lines. No forms, no JS.
- `MedicalAudience` schema.org on /clinical/ targeting MedicalProfessional.

### 6 Phase 4 route stubs added
- `/safety/`, `/programme-design/`, `/operator-blueprint/` × EN + EL.
- Real content arrives in Phase 4. Routes exist now so /wellness/ + /clinical/ can link to them without 404s.

### Lighthouse — better than 3.A baseline
| Pillar | Mobile (3.A → 3.B) | Desktop (3.A → 3.B) |
|---|---|---|
| Performance | 98 → 98 | 100 → 100 |
| Accessibility | 95 → **100** (+5) | 95 → **100** (+5) |
| Best Practices | 100 → 100 | 100 → 100 |
| SEO | 66 (noindex) | 66 (noindex) |

`/clinical/` Lighthouse spot-check: 100 / 100 / 100 / 66. New layouts deliver consistent performance.

A11y regression caught + fixed mid-session: `Footer.astro` copyright `text-slate-400` had 2.51 contrast on `bg-slate-50` (below WCAG AA 4.5). Bumped to `text-slate-600` (~7:1, AAA). Bug was inherited from 2.D — 3.B's shorter homepage just made it visible to Lighthouse.

### Build status
- `astro check`: 0/0/0 across **69 files**.
- `pnpm build`: **99 routes** in 1.52 s.
- Sitemap: 98 routes (404 excluded), EN/EL alternates wired.

## Phase 3.C — done (this session)

### `docs/PHASE-4-PLAN.md` committed
- 10 sub-steps (4.A–4.J), ~37–48 sessions, 4 stop points.
- Scope locked to 5 PDF clusters + 3 standalone pages. Wider original-brief items proposed for deferral.
- DOI verification: automated `pnpm verify-refs` (preferred) or manual checklist fallback.
- Per-cluster PR + preview-deploy review workflow.
- Three open questions surfaced for Stamos's async review.
- **Source PDF arrived**: `docs/sources/Other HBOT applications.pdf` is now in the repo (Stamos committed it during this session). Phase 4.A can start with research material in hand once 3.D–3.F complete.

### 3.C content
- 7 reusable card components in `src/components/cards/`: `MechanismCard`, `IndicationCard`, `DepartmentCard`, `DeptWithoutCard`, `LongevityCard`, `StudyCard`, `ReferenceItem`. (These also satisfy most of Phase 3.F's "extract card primitives" — F's cleanup pass becomes verification rather than fresh extraction.)
- 8 index pages × 2 locales = 16 routes built:
  - `/mechanisms/` — 5-card grid + paradox callout
  - `/indications/` — 14 cards grouped into 5 category sections (no JS filter)
  - `/departments/` — 9-card grid + footer link to without-hbot
  - `/departments/without-hbot/` — 6-card grid + amber disclaimer banner
  - `/longevity/` — 6-card grid + Hyperoxic-Hypoxic Paradox callout
  - `/evidence/` — 8 study cards + level legend + summary banner
  - `/references/` — 24 reference items with `id="ref-N"` anchors (cross-link target for 3.E reverse index) + 4-stat summary
  - `/protocols/` — static comparison matrix (14-row FDA table + 6-row longevity table). Replaces the original interactive multi-select + jsPDF tool per Phase 3 plan's zero-JS direction; Cmd-P prints cleanly.
- `/about/` stays as 3.A stub — Phase 6 launch-prep fills it.

### Lighthouse spot-checks (mobile)
| Page | Perf | A11y | BP | SEO | FCP | LCP |
|---|---|---|---|---|---|---|
| `/indications/` | 100 | 100 | 100 | 66 | 1.5 s | 1.5 s |
| `/references/` | 98 | 100 | 100 | 66 | 1.8 s | 1.8 s |

All real-target pillars ≥95. Compressed page weight across all 8 index pages: 35–37 KB. Tight and consistent. Reports + summary at `docs/3c-lighthouse-summary.md`.

### Build status
- `astro check`: 0 / 0 / 0 across **76 files** (was 69 → +7 cards).
- `pnpm build`: 99 routes in 1.58 s.
- Quadruple-grep clean.

## What's next (waiting on Stamos)
1. **Async review of `docs/PHASE-4-PLAN.md`** — answer the three open questions on scope / DOI tooling / reviewable unit. Source PDF prerequisite already satisfied.
2. **3.C review** — visit:
   - https://hbot-resource.pages.dev/indications/ (category groupings)
   - https://hbot-resource.pages.dev/references/ (24-item bibliography)
   - https://hbot-resource.pages.dev/protocols/ (comparison matrix, try Cmd-P printability)
   - https://hbot-resource.pages.dev/departments/without-hbot/
   - Greek mirrors at `/el/...`
3. Approve **3.D** — detail pages via dynamic routes. 34 entries (5 mechanisms + 14 indications + 9 departments + 6 longevity) × 2 locales = 68 detail pages, each replacing its 3.A stub.

## Open issues / risks
- **Domain `hbotscience.org` registered (Namecheap), DNS at Cloudflare, awaiting activation.** Hard-coded in `src/lib/seo.ts` as the canonical URL. Single source of truth — if it changes, edit there and rebuild.
- **Cloudflare Pages connection deferred** — Stamos to set up via dashboard at sub-step 2.F.
- **`@astrojs/mdx` 5.x and `@astrojs/react` 5.x are available** but require Astro 6. We pinned to Astro 5.18.1 for stability. Revisit when Astro 6 is more battle-tested.

## Phase log
- **2026-04-28** — Phase 1 complete (inventory + baseline + content export + Phase 2 plan).
- **2026-04-28** — Phase 2.A complete (Astro shell + repo + initial commit pushed).
- **2026-04-28** — Domain corrected to `hbotscience.org`.
- **2026-04-28** — Phase 2.B complete (80 content entries migrated to YAML, de-branded, Zod-validated, build clean).
- **2026-04-28** — Phase 2.C complete (i18n ported with de-brand, Nav + Footer built, Red Cross stealth reference caught and dropped, all four forbidden patterns clean in shippable code).
- **2026-04-28** — Phase 2.D complete (9 section components rendering from content collections, long-scroll homepage in EN + EL, 80 content entries surfaced, gzipped pages 27/33 KB, build clean). Lighthouse-local blocked on Brave headless quirk; deferred to 2.F.
- **2026-04-28** — Phase 2.F complete (Cloudflare Pages connected, hbot-resource.pages.dev live, Lighthouse measured against HTTPS preview). Mobile Performance 81 flagged as below target — render-blocking font/CSS issue. Decision: defer Option C font fix into Phase 3.A.
- **2026-04-28** — Phase 3 plan written and approved (`docs/PHASE-3-PLAN.md`). 6 sub-steps (3.A through 3.F), 4 stop points, ~18 sessions, ~90 routes target. Zero React islands.
- **2026-04-28** — Phase 3.A complete (self-hosted fonts + inline critical CSS + 3 reusable layouts + 30 URL stubs / 76 routes). Mobile Performance lifted 81 → 98, FCP/LCP halved to 1.8 s. All real-target pillars ≥95. 93 pages now deploying. Existing long-scroll homepage preserved as fallback per amendment B.
- **2026-04-28** — Phase 3.B complete (homepage redesign · /clinical/ + /wellness/ landings · Nav redesign · 14 EL longevity strings backfilled into EN · 6 Phase 4 stubs · A11y contrast regression caught and fixed). Mobile A11y 95 → 100. Editorial-integrity precedent locked: never ship unsourced claims, even inherited ones, in either language. 99 routes deploying.
- **2026-04-28** — `docs/PHASE-4-PLAN.md` committed. 10 sub-steps, ~37–48 sessions, scope locked to 5 PDF clusters + 3 standalone pages.
- **2026-04-28** — Source PDF `docs/sources/Other HBOT applications.pdf` committed by Stamos. Phase 4.A prerequisite satisfied.
- **2026-04-28** — Phase 3.C complete (7 reusable card components · 8 collection index pages × 2 locales = 16 routes built). Lighthouse spot-checks on /indications/ and /references/: 100/100/100/66 and 98/100/100/66. Compressed page weights 35–37 KB across all 8 indexes.
- **2026-04-28** — Phase 4 plan amendments applied (PDF rename satisfied, 4.B extra-care note added, Greek glossary locked at end-of-4.B).
- **2026-04-28** — Phase 3.D complete (`Breadcrumbs.astro` + `src/lib/refs.ts` cross-ref validator + 4 dynamic-route detail templates × 2 locales = 8 files generating 68 routes). Lighthouse on `/indications/refractory-osteomyelitis/`: 100/100/100/66 mobile + desktop after a11y contrast fix on inline text links (text-teal-700 hover:underline → always-underline pattern, applied across 12 files). FCP/LCP 1.0 s mobile. Proceeding to 3.E.
- **2026-04-29** — Phase 3.E complete (`src/lib/cross-links.ts` reverse-index helper · `src/lib/breadcrumbs.ts` shared type · `RelatedAside.astro` aside component · BaseHead emits `BreadcrumbList` JSON-LD when `breadcrumbs` prop populated · DetailLayout refactored from slot to prop pattern · 8 detail pages updated · `/references/` ReferenceItem now shows "Cited in:" per entry). Sample reverse-index verified on refractory-osteomyelitis (5 related entries via shared refs `[1]` + `[11]`) and ref-9 (cited by 6 entries). Lighthouse 100/100/100/66 on detail page after fixing one more `text-slate-400` low-contrast bug in RelatedAside (third instance of same pattern → site-wide convention now: `text-slate-500` minimum for muted microcopy on light backgrounds).
- **2026-04-29** — **Phase 3.F complete (Phase 3 closed).** Deleted 10 obsolete components (`HomePage.astro` + `sections/*.astro` × 9), 1,094 LOC removed; src/ Astro+TS files 80 → 70. Build still 99 routes / 1.6 s. Final quadruple-grep clean (one stray match in a `global.css` comment cleaned in same pass — CSS is inlined and would have shipped to every visitor). 5-path cross-link end-to-end validation surfaced one bug: EN `/longevity/[slug]/` was missed in 3.E's batch update so all 6 EN longevity Related asides were empty; fixed by bringing EN pattern to parity with EL. Final mobile Lighthouse sweep on 4 representative pages: `/` 100/100/100/66 · `/clinical/` 98/100/100/66 · `/wellness/` 98/100/100/66 · `/indications/refractory-osteomyelitis/` 99/100/100/66. All real-target pillars ≥95 across the site. **Phase 3 done. Phase 4.A starts next.**
- **2026-04-29** — **Phase 4.A complete.** `scripts/verify-references.mjs` validates DOIs via CrossRef API (bypasses publisher anti-bot). Wired into pnpm build via `prebuild` hook — Cloudflare build gate confirms all 24 DOIs resolve before any deploy. **2 inherited DOIs caught + fixed:** ref-16 (`0963689719874637` → `0963689719853540`, vascular dementia paper) and ref-19 (`OPTH.S276591` → `OPTH.S224192`, central retinal artery occlusion review) — both typo'd in the Henry Dunant fork data. Located correct DOIs via PubMed search, verified via CrossRef. 3 new dept entries committed: dermatology (orange #ea580c, Bandage), plastic-surgery (pink #db2777, Sparkles), womens-health (purple #9333ea, Venus) — applications: [] until 4.D/4.E/4.F populate. Empty-applications fallback notice added to dept detail pages (EN + EL). Order list updated in 4 places (departments + clinical landings × 2 locales). 99 → 105 routes. Mobile Lighthouse on `/departments/` (12 cards now): 98/100/100/66.
- **2026-04-29** — **Phase 4.B opened (PR #1).** Long COVID indication added: 4 new references (ref-25 to ref-28, all CrossRef-verified), neurology dept gains 6th application, full audit trail in `docs/research/long-covid-notes.md`, evidence level B with Stamos-approved framing. **First Cloudflare deploy of the PR failed** — verifier surfaced ref-15 as a publisher URL masquerading as a DOI (cached 200 locally, 403 on Cloudflare). Hardened the verifier to classify `doi` field input (bare DOI / canonical doi.org URL / known non-DOI repository host) and reject publisher-URL DOI traps with a clear error. **5 inherited-data corrections surfaced in this iteration:** ref-15 malformed DOI + wrong authors/year (Wolf 2012 → Figueroa-Wright 2016) · study-6 mirrored same paper, same fixes · ref-10 nature.com URL → canonical doi.org · ref-18 phantom citation (claimed *Nitric Oxide* 2023 with bogus PII, actually Cell Stress and Chaperones 2015) · CRAO indication misapplied [15] → corrected to [19] (Celebi 2021). Pattern reinforced: editorial-integrity tools added for new content retroactively validate old content — defence in depth working as designed.
- **2026-04-29** — **Phase 4.B merged to main (`c276fd7`).** Stamos approved Long COVID content on live preview. **Pre-merge structural fix landed** in same PR: `/indications/` index page mislabelled itself "FDA-Approved Indications" — would have categorised every Phase 4 emerging-evidence indication under FDA approval. Indications schema gained `tier: "fda-approved" | "emerging"` axis (orthogonal to existing medical-domain `category`). Index page now splits into two h2 sections (`#fda-approved` and `#emerging`) with anchor IDs; detail breadcrumbs read tier-aware (`Home › Indications › FDA-Approved › Refractory Osteomyelitis` vs `Home › Indications › Emerging Evidence › Long COVID`); BreadcrumbList JSON-LD picks up the new structure via BaseHead pass-through. EN+EL parity. Editorial precedent: HBOT applications stratified at navigation level by FDA-approval status — mirrors Cochrane and UpToDate evidence stratification. **Greek terminology register locked at `docs/research/el-glossary.md`** per Phase 4 amendment C — binding for 4.C–4.J. **`RelatedAside` polish** landed: collection eyebrow + entry title were rendering with insufficient visual separation; switched span/span to p/p with proper typographic spacing matching the IndicationCard pattern. **Cumulative editorial precedents from Phase 4.B: 6 inherited integrity issues surfaced and resolved** (3 malformed DOIs, 1 fabricated citation [ref-18], 1 misapplied citation [CRAO `[15]`], 1 misleading taxonomy [`/indications/` umbrella mislabelled]). The verification infrastructure built in 4.A continues to retroactively validate inherited Phase 2.B data; each cluster review surfaces more.
- **2026-04-29** — **Three production bugs caught and fixed on main.** Surfaced by Stamos on first post-merge review of the live site. (1) Reference tag badges on detail pages emitted `href="#ref-N"` (same-page anchor) — broken since Phase 3.D, but invisible until clinical readers actually clicked them; fixed by adding a `prefix` prop and emitting the cross-page form `${prefix}/references/#ref-N`, threaded through all 8 callsites (mechanisms/longevity/indications/departments × EN+EL). (2) `/references/` page subtitle was hardcoded "All 24 peer-reviewed sources..." — stale since 4.A's two new dept refs and 4.B's four new long-COVID refs took the count to 28; fixed by extending the `t()` helper to support `{name}` template substitution and passing `{count: total}` from the page. (3) Stat regex `/RCT|Meta-Analysis/i` undercounted the strongest-evidence bucket because it didn't match the spelled-out form "Randomized Controlled Trial" — fixed; bucket count went 4 → 7. Stat label "Systematic Reviews" was a misnomer (regex matched any review type) — renamed to "Reviews & Meta-Analyses" for accuracy. ReferenceItem badge regex fixed to match. **Editorial precedent reinforced: navigation that doesn't work and stats that contradict the page content erode reader trust on first visit; cross-link infrastructure needs both ends (anchor IDs + cross-page hrefs) to be testable in the build, not just by visual inspection.**
- **2026-04-29** — **Phase 4.C complete (PR #2 merged as `4bc15f9`).** Fibromyalgia indication added: 4 new CrossRef-verified references (ref-29 Efrati 2015 PLoS One foundational trial · ref-30 Cao 2023 Clin Pract independent meta-analysis · ref-31 Chen 2023 BMJ Open SR/MA · ref-32 Ablin 2023 PLoS One TBI-FM RCT), neurology dept gains 7th application, full audit trail in `docs/research/fibromyalgia-notes.md`. Evidence level B; the single-research-group caveat is **softened** (not removed) because two independent meta-analyses provide meta-analytic confirmation, but original positive RCT remains anchored to the Efrati lab. Source PDF disposition: 7 cited references collapsed to 4 unique papers (PDF [10]=[11] Efrati cited via PMC + PLOS URLs; PDF [13]=[14] Chen cited via PMC + BMJ URLs); PDF [16] excluded as clinic-blog popular media (same logic as Yale Medicine in 4.B). **New medical-domain category** "Chronic Pain Syndromes" / "Σύνδρομα Χρόνιου Πόνου" introduced — Long COVID stays under "Post-Viral Syndromes" (Option B from 4.B open-questions: medically-honest categories per indication, all under tier umbrella). **Editorial review iteration:** TBI-FM RCT given an explicit description sentence per Stamos's edit during PR review; ΤΕΔ first-use convention preserved by swapping expansion location into the new sentence (now first occurrence) with bare ΤΕΔ in the caveat (second occurrence). **Greek glossary refined:** 11 new terms locked in § 4.C of `docs/research/el-glossary.md` (fibromyalgia, central sensitisation, tender points, widespread pain, quality of life, function, brain activity patterns, prospective controlled trial, pooled randomised data, magnitude of effect, meta-analytic confirmation, supporting-evidence variant of single-research-group caveat). 32 references in collection; 109 routes deployed.
- **2026-04-29** — **Phase 4.D complete (PR #4 merged as `68a5f8e`).** First new-department activation. Inflammatory Dermatoses added: 4 new CrossRef-verified references (ref-33 Goggins 2019 Acta Dermatovenerol APA — bridges FDA NSTI/grafts and emerging HS/PG · ref-34 Zwoliński 2025 J Clin Med atopic dermatitis review · ref-35 Mews 2021 J Clin Med paediatric AD study · ref-36 Butler 2009 J Med Case Reports psoriasis cases). Dermatology dept moves from `applications: []` (placeholder set in 4.A) to 5 entries: 4 cross-referencing existing FDA indications (Chronic Wounds, Radiation Dermatitis, Compromised Grafts/Flaps, Burns + NSTI) + 1 pointing to the new Inflammatory Dermatoses indication. **First Level C indication of Phase 4** — case-level + small-cohort evidence; "adjunctive, not first-line" framing. **Scope adjusted post-PDF reading**: original Phase 4 plan named 4 dermatology indications (Burns extension, Pyoderma gangrenosum, Necrobiosis lipoidica, Skin grafts) written before the source PDF was fully analysed; three are already in the FDA-approved set and the fourth is not in the PDF. Replaced with one umbrella indication + dept activation. Recorded at `docs/PHASE-4-PLAN.md` line 46+ via commit `162d3a7`. Precedent: source PDF anchors scope; same check to be applied at 4.E and 4.F. **EL parity fix landed in same PR (commit `4d4c7ae`)**: Stamos surfaced two EL/EN regressions on review — protocol panel English qualifiers leaking into EL (sessions + frequency were single-string fields; values like "highly variable", "Daily, protocol-dependent", "5×/week" rendered identically in both locales) and inflammatory-dermatoses EL eyebrow collapsing umbrella vs indication name. Fixed structurally: schema migration promoted `protocol.sessions` and `protocol.frequency` to `bilingual`; `scripts/migrate-protocol-bilingual.mjs` (one-shot, idempotent, with explicit translation table) applied to all 33 protocol-block-bearing YAMLs (17 indications + 10 departments + 6 longevity); ProtocolPanel.astro reads `[locale]`; EL category for inflammatory-dermatoses revised to "Φλεγμονώδεις Δερματικές Παθήσεις". Glossary § 5 expanded with 13 canonical EN→EL pairs for protocol vocabulary. Future Phase 4 clusters inherit the bilingual schema. 36 references; 111 routes.
- **2026-04-29** — **Phase 4.E–4.J continuous run complete** on branch `phase-4/run-e-to-j` (single PR for entire run; Stamos authorised minimised review cycles).
  - **4.E Aesthetic & Reconstructive Recovery** (PMC10387739 facelift case-control, 13.3 vs 36.9 days, P<.001 + Francis 2017 review + Simman 2022 case series + Bassetto 2019 lit review). Plastic surgery dept activated 5 apps. **Level B** per Stamos's call (Neel 2023 case-control gives real comparative outcome).
  - **4.F Endometriosis only** (fertility omitted per Stamos; menopause already omitted). Translational evidence (Aydin 2011 rat + Syahrizal 2022 mouse) + HEROES registered trial (Sunnybrook NCT06579040, recruiting since 2025-04). Women's health dept activated 2 apps. **Level C investigational**. Editorial-integrity catch: source PDF cited two commercial sites for "pilot study" claim — PubMed search confirmed no human pilot study exists, so the claim was dropped and the description re-framed around translational + registered-trial evidence with explicit "clinical evidence in humans is awaited". Verifier extended to accept `clinicaltrials.gov` as a recognised non-DOI repository host.
  - **4.G /safety/ standalone page** (EN + EL): absolute and relative contraindications, adverse events (Heyboer 2017 review backbone), pre-treatment screening, stop criteria. New ref-44 Heyboer 2017 *Adv Wound Care*. Clinical reference register; explicit not-replacement-for-patient-assessment disclaimer.
  - **4.H /programme-design/ standalone page** (EN + EL): operator guidance for ethical, evidence-based wellness programmes — patient screening, protocol selection (cited to Hadanny 2020 cognitive RCT + telomere/senescence + athletic), frequency norms, outcome measurement (validated tools only), claim boundaries (no anti-cancer / autism / DNA-repair / lifespan claims), compliance and adherence.
  - **4.I /operator-blueprint/ standalone page** (EN + EL): organisational architecture — equipment selection (hard vs soft chambers, monoplace vs multiplace, with explicit note that 1.3 ATA soft-shell deployments cannot reproduce 2.0 ATA RCT protocols), staffing, facility (NFPA 99 reference for fire safety), quality control, patient flow + consent, documentation. No commercial pricing, no IN2050 product names; footer-only IN2050 attribution preserved.
  - **4.J QA** all clean: quadruple-grep across `src/` + `dist/` + `public/` for Henry Dunant / Νοσοκομείο Henry / Ντυνάν / Ερυθρός Σταυρός = 0 matches; `pnpm verify-refs` = 44/44 ✓ via CrossRef + recognised non-DOI repos; `astro check` = 0 errors / 0 warnings / 0 hints; `pnpm build` = 115 routes (was 111 → +4 for the 2 new emerging indications × 2 locales).

## Phase 5 polish backlog (deferred from Phase 4 run)

Items deliberately not addressed during the 4.E–4.J continuous run (per Stamos's standing rule 8: no mid-run pauses for minor issues). Each is non-blocking; tackle in Phase 5 polish:

- **Homepage stats**: "2–3" should read "2.0–3.0 ATA"; FDA-Approved tile copy needs both-tier framing (no longer just FDA); Bibliography tile stale count (now 44, may be hardcoded somewhere)
- **Greek font self-hosting**: Noto Sans Greek to match Latin self-hosting from Phase 3.A
- **Mobile language-toggle redundancy**: Nav and Footer both expose it; consolidate on mobile
- **Visual identity refinement**: teal palette inherited from Henry Dunant; colour palette review for full re-brand
- **Department colour palette review**: orange/pink/purple introduced in 4.A — review for cohesion with existing palette
- **Astro 6 upgrade evaluation**: 5.18.1 currently; check upgrade ROI
- **Codebase grep for hardcoded counts**: similar pattern to the 4.B "All 24" subtitle bug — sweep for any remaining hardcoded totals of any collection
- **Heading-hierarchy a11y**: medical-domain category h3s and indication-card title h3s share a level under the tier h2 — defensible but fix in polish
- **Cross-link `RelatedAside` density check**: with 4 new emerging indications + 3 new dept activations + 8 new refs cross-linking into existing pages, verify the asides don't visually overflow on any detail page
- **Trial Registration entry rendering**: ref-43 (HEROES NCT06579040) is `type: "Trial Registration"` — check the `/references/` page renders this type with appropriate badge styling alongside RCT/Review/Guideline types
- **2026-04-29** — **Phase 4 MERGED to `main` (PR #5, merge commit `4d0f077`).** Stamos approved the full 4.E–4.J continuous run. Phase 4 is content-complete: 19 indications (14 FDA + 5 emerging), 12 departments fully populated, 44 references verified, 3 standalone pages live (safety / programme-design / operator-blueprint), 115 routes total. **Awaiting Phase 5 plan from Stamos.** Phase 5 backlog (10 items, listed above) is the candidate set; final scope decided once Phase 5 plan lands.
- **2026-04-29** — **Phase 5 polish PR #6 opened** (branch `phase-5/polish`, head commit `3290658`). Single PR addressing 10 of the 12 listed backlog items in one continuous run per Stamos's standing preference. **8 items shipped as code changes**: homepage stat 2.0–3.0 ATA · FDA tile renamed Indications with build-time count · Bibliography tile drops count · codebase audit for hardcoded counts (no further stale numbers found beyond the homepage two) · Greek font self-hosting via @fontsource/noto-sans (weights 400/500/600/700) · Footer language toggle removed (Nav sticky-top is sufficient) · SectionHeader page heading promoted to h1 (was inherited Phase 3 oversight on collection index pages) · medical-category eyebrows in `/indications/` index demoted from h3 to styled `<p>` · ReferenceItem typeBadge gains amber for Trial Protocol + Trial Registration. **2 items decided without code change**: RelatedAside density (already capped at 5 from Phase 3.E — confirmed working, no change needed) · Astro 6 upgrade (DEFERRED to Phase 7 — major version bump, breaking changes; migration risk not justified in a polish phase). **2 items surfaced as proposals**: visual identity (teal palette evaluation) + department colour palette review — both await Stamos's editorial decision; no code changes pending sign-off. **Lighthouse mobile** (Brave headless) on 5 representative pages (EN homepage, EL homepage, FDA indication, emerging indication, safety): Performance 98–100 / A11y 100 / Best Practices 100 / SEO 66 (the noindex penalty, lifts at Phase 6 launch). Greek font self-hosting did not regress Performance on the EL homepage. **Codebase-grep audit patterns documented for future use**: `Twenty-?(four|eight)`, `Thirty-?two`, `Fourteen`, `Sixteen`, `Eighteen`, `Nineteen`, `Forty-?four` (numeric words 14–50); regex `\\b(2[0-9]|3[0-9]|4[0-9])\\b.*(reference|indication|department|mechanism|longevity|source|paper|condition|peer-reviewed)` (digit-then-collection-noun in i18n strings). Run quarterly or whenever a collection grows.
- **2026-04-29** — **Phase 5 polish merged to main** (PR #6, merge commit `d8ebd74`). 11 of 12 listed items shipped (1 deferred to Phase 7: Astro 6 upgrade). Final Phase 5 deliverables: homepage stat `2.0–3.0 ATA` · FDA tile body uses build-time `getCollection().length` · Bibliography tile drops the count · codebase grep audit patterns documented (run quarterly) · Greek font self-hosted via `@fontsource/noto-sans` Greek subset (weights 400–700) · Footer language toggle removed (Nav sticky-top sufficient) · `SectionHeader` page heading promoted h2→h1 (collection index pages now have proper h1) · medical-category eyebrows in indications index demoted h3→`<p>` · `ReferenceItem` typeBadge gains amber for Trial Protocol + Trial Registration · dept palette toned to muted variants (Stamos's Option B) · Nav + Footer logo swap to `public/brand/hbot-science-logo-{horizontal,circular}.svg` (above-fold horizontal logo gets fetchpriority=high; below-fold circular gets loading=lazy). Lighthouse mobile post-deploy: Performance 100/100/100/100/100 across homepage EN+EL + FDA detail + clinical + wellness landings; LCP 0.21–0.33s; SEO 69 (still noindex-penalised; lifts at launch).
- **2026-04-29** — **Phase 6 launch begun.** Pre-flight checks pending Stamos's dashboard actions. Flow: (1) Stamos attaches `hbotscience.org` to Cloudflare Pages project; (2) Claude flips `noindex={true}` off across the 39 page-level callsites (robots.txt already points to hbotscience.org sitemap); (3) Stamos submits sitemap to Google Search Console + Bing Webmaster Tools; (4) Claude smoke-tests on production domain; (5) `/about/` editorial-policy content collaborative with Stamos. Step 2 must wait for step 1 — otherwise Google may index the `.pages.dev` preview URL.
- **2026-04-29** — **Phase 6 launch CLOSED. Site is live at https://hbotscience.org.** Step-by-step:
  - **Step 1** (Stamos): hbotscience.org + www.hbotscience.org attached to Cloudflare Pages, SSL active.
  - **Step 2** (Claude, commit `764d717`): noindex flipped off across 37 of 39 page-level callsites — left noindex on the two /about/ stubs pending step 5. Production hbotscience.org/ confirmed indexable; /about/ confirmed still noindexed.
  - **Step 3** (Stamos): sitemap https://hbotscience.org/sitemap-index.xml submitted to Google Search Console (Domain property; full URL needed for Domain properties — initial attempt with relative path failed) and Bing Webmaster Tools.
  - **Step 5** (collaborative, commit `eb25b8c`): /about/ EN + EL written collaboratively — editorial standards, evidence framework (FDA-approved vs emerging-evidence tiers), publisher disclosure (IN2050 commercial-interest acknowledgement), corrections process (mailto:info@hbotscience.org), scope and limits. AboutPage JSON-LD. noindex prop removed. Both /about/ pages now indexable. Cloudflare Email Address Obfuscation rewrites the mailto: link at the edge into /cdn-cgi/l/email-protection — link works via injected JS for end users; noted as a Phase 7 toggle if Stamos wants raw mailto.
  - **Step 4** (Claude, smoke test on production): 41/41 routes HTTP 200 · quadruple-grep across 7 representative pages clean · sitemap-index serves valid XML pointing to sitemap-0.xml with 114 URLs all at hbotscience.org · BreadcrumbList JSON-LD validates EN+EL on /indications/long-covid/ · zero noindex meta tags on indexable pages · Lighthouse mobile 100/100/100/100 across homepage EN+EL + FDA detail + emerging detail + about, LCP 0.12–0.24s. **First time in the project's history hitting SEO 100 — the noindex penalty during preview phase had capped it at 66–69 since Phase 2.A.**
- **Phase 7 backlog (not committed, log only)**: Astro 6 upgrade evaluation (deferred from Phase 5) · visual identity / brand-level evaluation (deferred from Phase 5) · Cloudflare email-obfuscation toggle decision · www→apex 301 redirect (currently both serve 200 independently) · multilingual expansion DE/IT/AR (separate Stamos directive required).
- **2026-04-30** — **Phase 7 (German rollout) complete on local branch.** Single continuous run per Stamos's directive. Stages 1–7:
  - **Stage 1 — Infrastructure.** `Locale` type widened to `"en" | "el" | "de"`. Astro i18n config gains `de`. Schema (`src/content.config.ts`): `bilingual` + `bilingualStringArray` go from `{en, el}` to `{en, el, de}` with DE `.optional()` during the rollout, `.required()` at Stage 6. BaseHead emits `<link rel="alternate" hreflang="de">` plus `og:locale=de_DE`. Nav language switcher rewritten from binary toggle to N-locale loop using `SUPPORTED_LOCALES.filter()`. Footer toggle had been removed in Phase 5 — no change needed. Hardcoded `{en, el}` label maps in `ProtocolPanel.astro` and `RelatedAside.astro` extended with `de` entries; `ReferenceItem.astro` `citedInLabel` extended.
  - **Stage 2 — UI strings.** `src/i18n/de.ts` written by translating every key in `src/i18n/index.ts` (~150 keys covering nav, sections, footer, breadcrumbs, badges, CTAs).
  - **Stage 3 — Content.** Trilingual translations applied to **every YAML in every collection**: 19 indications + 12 departments × applications[] + 5 mechanisms + 6 longevity entries + 44 references (titles/authors/journals/DOIs preserved; type stays English per existing EL precedent; new `citedInLabel` localised) + 3 standalone pages (safety / programme-design / operator-blueprint, each ~5 sections of medical content) + /about/ editorial policy + 6 departments-without-hbot + 8 research-studies + 4 strategic-recommendations + 4 site-stats. **Greek-leak grep on built `dist/de/`** = 0 functional leaks (only the language-switcher label "Ελληνικά" linking to EL, intentional). All 19 DE Astro page files (collection indexes, [slug] dynamic routes, standalone landings) translated from EL stubs to German hard-coded copy: hero text, breadcrumbs, headings, CTAs, JSON-LD `inLanguage` codes, asides.
  - **Stage 4 — Glossary.** `docs/research/de-glossary.md` flipped from DRAFT to APPLIED — terms have been used across the production DE site; native-speaker medical review remains a Phase 8 deliverable (cluster-by-cluster lock-in, same posture as the EL glossary went through). 263 lines, 7 sections, ~120 EN→DE pairs.
  - **Stage 5 — Long COVID register.** DE Long COVID translation set the hyperbar-medical register: `Long COVID (Post-COVID-Syndrom)` for the condition, `Aufkommende Evidenz` for the tier, `RKS` (Randomisierte Kontrollierte Studie) for RCT first-use throughout (mirrors Greek `ΤΕΔ` first-use convention with the German equivalent abbreviation). HOT-LoCO trial caveat preserved in DE. `„Brain Fog"` rendered with German Anführungszeichen and ASCII-quote-escaped to keep YAML parseable.
  - **Stage 6 — QA.** Schema locked: `de: z.string()` (required) replaces `.optional()`. `pnpm verify-refs` = 44/44 ✓ from cache. `astro check` = 0 errors / 0 warnings / 0 hints (after fixing 5 stale TypeScript types in `ProtocolPanel.Protocol`, `RelatedAside.CrossRef`, `ReferenceItem.CrossRef`, all of which had hardcoded `{en, el}` from Phase 4.D — bumped to `{en, el, de}`). `pnpm build` = **172 routes** (was 115 EN+EL only → +57 DE pages). Quadruple-grep on `dist/` = 0 Henry Dunant leaks across all locales. **Pre-existing bug surfaced and fixed during the rollout: `/protocols/` page rendered `[object Object]` for `sessions` and `frequency` columns on EN + EL production since Phase 4.D's bilingual schema migration** — `row.protocol.sessions` was passed as a whole object instead of `[locale]`-indexed. Fixed in all three locales (EN/EL/DE) — verified DE renders proper German text (`1 – 3 (notfallmäßig)`, `Einmal täglich, 5×/Woche`).
  - **Stage 7 — Report.** This entry. **Production Lighthouse not re-run for DE pages** because the deployment hasn't shipped to production yet (PR not opened); local build clean. The DE pages use the same templates and inlined CSS as the EN/EL pages currently scoring 100/100/100/100 on production, so a regression is structurally unlikely.
  - **Editorial-integrity catches surfaced this run:** (1) protocols `[object Object]` bug on EN + EL since Phase 4.D — fixed across all locales; (2) `de-glossary.md` was DRAFT-status starter pack that I shipped against without native review — header rewritten to APPLIED-but-pending so the editorial trail is honest. **Phase 8 follow-ups recorded**: native German clinician review of `de-glossary.md` cluster-by-cluster lock; native German review of every DE-translated YAML (mirrors the EL editorial-review cadence); decision on whether to localise the references `type` field across all 3 locales (currently English-only by design — same as EL).
  - **Repo state**: branch `main`, working tree carries DE rollout uncommitted. Open PR: 0. Awaiting Stamos's review on whether to commit + open PR, or revise.
- **2026-04-30** — **Phase 7 (German) shipped (PR #7, merge `2ab04c6`).** 3-commit shape (infrastructure / DE content / `[object Object]` protocols fix). Cloudflare preview verified all 6 sampled DE routes 200; Lighthouse mobile `/de/` + `/de/indications/long-covid/` = 100/100/100/69 (SEO 69 = preview noindex; 100 on production). The protocols `[object Object]` fix verified live across EN/EL/DE. Endometriose ata/duration English-leak fix followed (schema-level: `protocol.ata` + `protocol.duration` promoted single-string → bilingual across all 33 protocol-bearing YAMLs; "Not standardised" → "Μη τυποποιημένο"/"Nicht standardisiert"). **Cumulative editorial-integrity catches: 14.**
- **2026-04-30** — **Globe language switcher + hero cleanup shipped (PR #8, merge to main).** Per-locale link strip replaced with a single `lucide:globe` dropdown (native `<details>`, ~10-line inline script for click-outside + Escape; scales to N locales). New i18n key `nav.changeLanguage`. Homepage hero language-coverage sentence dropped in all locales (the globe communicates it; the phrase was stale-prone and inconsistent). Lighthouse `/de/` 100/100/100/69 on preview (warm-cache).
- **2026-04-30** — **Phase 7.B (Italian rollout) complete on local branch `phase-7b/italian`.** Single continuous run per Stamos's directive. Four locales now: EN (root) · EL (`/el`) · DE (`/de`) · IT (`/it`). Stages 1–7:
  - **Stage 1 — Infrastructure.** `SUPPORTED_LOCALES` → `["en","el","de","it"]` (the globe switcher picks IT up automatically). Astro i18n config + sitemap gain `it`. Schema (`src/content.config.ts`): `bilingual` + `bilingualStringArray` gain `it` — `.optional()` during Stages 1–5 (warn-not-fail), flipped to required at Stage 6. BaseHead emits `hreflang="it"` + `og:locale=it_IT`. Nav `localeNames`/`navLabels`/`isActive`, Footer about-link, StubContent, ProtocolPanel (label map + `Protocol` type), RelatedAside (`collectionLabel`), ReferenceItem (`citedInLabel`), and `CrossRef.title` type all extended with `it`.
  - **Stage 2 — UI strings.** `src/i18n/it.ts` — all 150 keys translated (SIMSI register). Decimal comma in prose (2,0 ATA); HBOT kept (not OTI); `«caporali»` quotation marks.
  - **Stage 3 — Content.** `it` field added to **every multilingual field in every collection** (de/it line-count parity verified exactly): 19 indications + 12 departments × applications[] (332 fields) + 5 mechanisms + 6 longevity + 8 research-studies + 6 departments-without-hbot + 4 strategic-recommendations (incl. `actions[]` arrays) + 4 site-stats. References (44) untouched — English-language-neutral by design, `citedInLabel` localised in-component (same precedent as EL/DE). All 19 IT Astro page templates created mirroring the DE pages (collection indexes, `[slug]` dynamic routes, standalone landings) with Italian hard-coded copy + JSON-LD `inLanguage="it"`. Executed via four parallel content agents + three parallel page agents, each briefed with the glossary + locked terminology decisions.
  - **Stage 4 — Glossary.** `docs/research/it-glossary.md` flipped DRAFT → APPLIED; section 7 rewritten to record the rollout decisions that resolved the draft's open items (OTI→HBOT, fatigue→affaticamento, case report→caso clinico, RCT first-use convention, compromised→compromesso, modello murino disambiguation) plus the indication/category names coined during translation. Native Italian clinician review remains a Phase 8 deliverable (same posture as EL/DE).
  - **Stage 5 — Fonts.** Existing self-hosted Latin stack (Montserrat/Lato) covers Italian accented characters (à è é ì ò ù) — verified, no subset added.
  - **Stage 6 — QA.** Schema locked (`it: z.string()` required). `astro check` = 0/0/0 across 111 files (the 9 non-null assertions a page agent had added as an optional-schema workaround were removed once the schema became required — IT pages now match the DE pattern exactly). `verify-refs` = 44/44 ✓. `npm run build` = **229 routes** (was 172 → +57 IT). **Quintuple-grep** (`src` + `dist` + `public`) for Henry Dunant / Ντυνάν / Ερυθρός Σταυρός / Krankenhaus / Ospedale / Red Cross / Croce Rossa = 0 live hits (one match is a code comment in `el.ts` documenting a *past deletion*). Long COVID register verified: caveat preserved at full force ("è in attesa una replicazione indipendente"), RCT first-use expansion present, `basis` converts ΤΕΔ/RKS→RCT, clean protocol phrases. **Lighthouse on local `astro preview` gave depressed numbers (home 91, FDA 84) — a known localhost artifact (no Brotli/HTTP-2/edge cache); production Lighthouse runs on the Cloudflare preview (as DE's did) for the accurate ≥95 reading.**
  - **Editorial-integrity note:** unlike the DE run, no latent cross-locale bug surfaced — the `[object Object]` and ata/duration leaks were already fixed at schema level during Phase 7, so the IT data simply inherited correct rendering. **Cumulative editorial-integrity catches: 14 (unchanged).**
  - **Phase 8 follow-ups recorded**: native Italian clinician review of `it-glossary.md` cluster-by-cluster lock; native Italian review of every IT-translated YAML + the 4 prose-heavy standalone pages (safety / about / programme-design / operator-blueprint); reviewer decision on OTI-vs-HBOT for the IT audience.
  - **Stage 7 — Report.** Delivered to Stamos. Awaiting review of live IT content before merge.
- **2026-05-29** — **Phase 7.B (Italian) shipped (PR #9, merge `9f5b498`).** 229→ routes; verified live on production.
- **2026-05-29** — **Research Digest feature shipped (PR #10, merge `a5e04fe`).** New EN-only `/digest/` section: archive + individual issue pages, reusable localised SubscribeForm (Turnstile-gated), and the site's first Cloudflare Pages Function (`functions/api/subscribe.ts`, Buttondown proxy). `enOnly` flag added to Nav + BaseHead + BaseLayout + IndexLayout so the EN-only section emits clean hreflang + a homepage-targeted switcher. verify-refs hardened (404/410 hard-fail; network/anti-bot soft-warn). Inaugural digest issue (`7beb50e`) backfilled with 24 Q2-2026 papers (all DOIs independently verified: 23 live + 1 pending-pub).
- **2026-05-29** — **Phase 7.C (Spanish rollout) complete on local branch `phase-7c/spanish`.** Single continuous run per Stamos's directive. Five content locales now: EN (root) · EL (`/el`) · DE (`/de`) · IT (`/it`) · ES (`/es`). Digest stays EN-only (out of scope). Stages 1–7:
  - **Stage 1 — Infrastructure + glossary seed.** `SUPPORTED_LOCALES` → `[…,"es"]` (globe switcher picks ES up as "Español" automatically). Astro i18n config + sitemap gain `es`. Schema: `bilingual` + `bilingualStringArray` gain `es` — `.optional()` during Stages 1–5 (warn-not-fail), flipped to required at Stage 6. BaseHead hreflang="es" + og:locale=es_ES. Nav/Footer/StubContent/ProtocolPanel/RelatedAside/ReferenceItem/CrossRef extended. **`docs/research/es-glossary.md` created from scratch** (no prior draft) and seeded with locked decisions: neutral/international Spanish (RAE); **decimal POINT** (not comma — diverges from IT); **HBOT** kept (not OHB); **ECA** for RCT first-use; «comillas»; FDA phrasing; tier labels.
  - **Stage 2 — UI strings.** `src/i18n/es.ts` — all 159 keys, structurally identical to en.ts.
  - **Stage 3 — Content.** `es` added to every multilingual field in every collection; it/es line-count parity exact: 19 indications (171) + 12 departments incl. applications[] (332) + 6 longevity (54) + 6 departments-without-hbot (18) + 5 mechanisms (15) + 8 research-studies (16) + 4 strategic-recommendations incl. actions[] (12) + 4 site-stats (8). References (44) untouched. 19 ES Astro page templates created mirroring the IT pages. Executed via four parallel content agents + three parallel page agents, each briefed with the glossary + locked decisions.
  - **Stage 4 — Glossary build-out.** Terminology decisions recorded across es-glossary §0–§7 with rationale; open items flagged for the native reviewer (decimal-point, HBOT-over-OHB, «comillas», "Servicios" for Departments, "niebla mental" for brain fog).
  - **Stage 5 — Fonts.** Existing self-hosted Latin stack covers á é í ó ú ñ ü ¿ ¡ — verified in built dist, no subset added.
  - **Stage 6 — QA.** Schema locked (`es: z.string()` required). astro check 0/0/0 (the 10 transient optional-schema ts errors on es pages cleared once the schema became required — no non-null assertions needed). verify-refs 44/44. build **288 routes** (was 231 → +57 ES, exactly the IT footprint). Leak-grep (src+dist+public) clean for Henry Dunant / Ντυνάν / Ερυθρός Σταυρός / Krankenhaus / Ospedale / Hospital Henry Dunant / Cruz Roja / Red Cross — only the documented el.ts code comment. Long COVID ES verified: caveat at full force ("se sustenta actualmente en un único grupo de investigación; se espera una replicación independiente"), ECA first-use expansion, basis RCT→ECA, decimal point, clean protocol phrases. Switcher cycles EN↔EL↔DE↔IT↔ES with ES current on /es/. /es/protocols/ = 0 `[object Object]`.
  - **Editorial-integrity note:** no latent cross-locale bug surfaced this run — the `[object Object]` and ata/duration leaks were already fixed at schema level in Phase 7, and the verify-refs flakiness was fixed during the digest work, so ES inherited correct behaviour. **Cumulative editorial-integrity catches: 14 (unchanged).**
  - **Phase 8 follow-ups:** native Spanish clinician review of `es-glossary.md` + every ES YAML + the prose-heavy standalone pages; confirm decimal-point, HBOT-over-OHB, «comillas», and the "Servicios"/"niebla mental" choices.
  - **ES polish backlog:** none blocking.
  - **Stage 7 — Report + PR.** PR `phase-7c/spanish` opened; awaiting Stamos's review of the live ES preview before merge.
- **2026-05-29** — **Phase 7.C (Spanish) shipped (PR #11, merge `22e9a35`).** 288 routes; verified live on production.
- **2026-05-29** — **Clinical/Wellness cleanup shipped (PR #12, merge `d6f17cd`).** Clinical grid → teaser + consultation CTA removed; Wellness merged into Longevity (5 pages deleted, operator block folded in, 301 redirects via `public/_redirects`); nav Wellness item removed; homepage card repointed. 283 routes. **Latent fix:** pre-existing `/longevity/` heading-order skip (LongevityCard `h3`→`h2`) repaired → A11y 100.
- **2026-05-29** — **`verify-digest` DOI gate + Clinical spacing + switcher order (branch `feat/verify-digest-clinical-spacing-lang-order`).** Three bundled changes:
  - **Part A — digest DOI gate.** Shared DOI/URL primitives extracted to `scripts/lib/doi-check.mjs` (DRY); `verify-references.mjs` refactored to import them (policy unchanged); new `scripts/verify-digest.mjs` scans `src/content/digest/**` `items[].url` with a pending-publication-tolerant tiered policy: CrossRef 2xx → pass; CrossRef-missing but doi.org resolves → SOFT WARNING (pending pub); doi.org 404/410 → HARD FAIL; network/anti-bot → retry 3× then soft warn. Non-DOI URLs get a lightweight reachability check (404/410 hard fail, else soft warn). Wired into `prebuild` (`verify-refs && verify-digest`) so it runs on every Cloudflare deploy. Inaugural issue: **23 pass + 1 soft warning** (`10.1093/occmed/kqag011`, pending pub); hard-fail path tested with a fabricated DOI (exit 1) and reverted.
  - **Part B — Clinical spacing.** Trimmed the teaser section's top padding (`py-16 md:py-20` → `pt-8 pb-16 md:pt-10 md:pb-20`) in all 5 locales — the void above the "Department application mapping" heading was the old grid section's padding wrapping a now-slim teaser. No content/structure change; library section untouched.
  - **Part C — switcher order.** `SUPPORTED_LOCALES` reordered to `["en","de","it","es","el"]` (Greek last; EN first/default). Cosmetic only — audit confirmed no positional dependency (default-locale logic is explicit `=== "en"`; hreflang hardcoded in BaseHead; Locale union order-independent).
  - **Editorial-integrity catches cumulative: 16.** Corrected from the 14 carried through the IT/ES entries: **+1** the inaugural-digest verify-refs coverage gap (24 DOIs had shipped unguarded — closed by Part A), **+1** the `/longevity/` heading-order skip fixed in PR #12.
- **2026-05-29** — **PTSD emerging-tier indication added (branch `feat/ptsd-indication`).** New emerging indication Post-Traumatic Stress Disorder (PTSD), evidence level B, investigational/not-FDA-approved, in all five locales. Introduced the **Neuropsychiatric** category (free-text bilingual `category` field — no enum, no route; surfaces as the detail-page eyebrow + a new grouping on the indications index, both auto). 4 verified references added (ref-45 Doenyas-Barak 2022 PLoS One; ref-46 Doenyas-Barak 2024 J Clin Psychiatry sham-controlled RCT; ref-47 Al-Shamali 2026 meta-analysis; ref-48 Levitt 2026 observational) — all resolve via CrossRef. Counts auto-updated everywhere (Phase 5 removed hardcoded counts): indications 19→**20**, emerging 5→**6**, references 44→**48**; site-stats (14 FDA / 258 citations) and the evidence page (research-studies + aggregate 8+/5+/258+) carry no per-indication count, so unchanged. PTSD renders correct tier/category/breadcrumb/level-B/investigational/protocol/4 refs across all five locales; figures verbatim (CAPS-5 42.6→25.8, 56/60/90/2 ATA/50/six months) with per-locale decimal separator; caveat (single research group + early independent corroboration) at full force. Glossary PTSD/neuropsychiatric terms appended to el/de/it/es (pending Phase 8 review). astro check 0/0/0; verify-refs gate exit 0 (47/48 + 1 uhms.org 503 soft-warn); verify-digest unaffected; build **288 routes** (+5: PTSD ×5; no category route). **Latent discrepancy surfaced (catch #17 candidate, flagged for Stamos):** the directive specified mapping PTSD to Psychiatry & Psychology and a "research indications 2→3" bump, but **Psychiatry already contains a PTSD application** (type Research; the dept already has 2 research apps: PTSD + Treatment-Resistant Depression). Per the non-negotiable dedup / additions-only / do-not-modify-existing rules, no duplicate PTSD card was added and the existing entry was not rewritten — psychiatry stays at 2. Recommend Stamos decide whether to refresh the existing dept PTSD entry to cite the new sham-controlled RCT (refs 45/46). PR opened; awaiting review.
  - **Follow-up (Stamos-approved, same branch/PR #14):** added a cross-link from the Psychiatry PTSD application to the new indication page via a new optional `indicationSlug` field on `departmentApplication` (additive schema field; the dept `[slug]` page renders the application title as a link when set). Set on the Psychiatry PTSD app → `/indications/post-traumatic-stress-disorder/`, applied in all five locale `[slug]` pages. **Research-application count correctly stays 2** (0 FDA / 2 research) in every locale — this is a link, not a new card. Evidence-text alignment (step 3) was **not required**: the existing dept PTSD descriptor (RCT evidence, treatment-resistant, Level B, "Research" badge) is already consistent with the indication page, so no wording change was made (psychiatry.yaml = 1 insertion: the `indicationSlug` line). **The directive-assumption discrepancy is now confirmed as editorial-integrity catch #17** (PTSD already existed as a Psychiatry application; the instructed 2→3 bump would have duplicated/miscounted — held at 2, correct). **Editorial-integrity catches cumulative: 17.**
- **2026-05-29** — **Rehabilitation Medicine department added (13th) — branch `feat/rehabilitation-department`.** New cross-cutting department presenting HBOT as a recovery-phase adjunct, 0 FDA / 4 Research, in all five locales. Step 0: Long COVID + Fibromyalgia indication pages **exist** → linked via `indicationSlug`; post-stroke + TBI/post-concussion **do not** exist as indication pages → verbatim evidence notes + references carried on the department page itself (no new indication pages this cycle). Icon **PersonStanding** (added to DepartmentCard + 5 `[slug]` icon maps). Placed **13th** (appended after womens-health) in the `order` array of all 5 dept-index pages. **Two additive infra extensions** (mirroring PR #14's `indicationSlug`): (1) optional `intro` bilingual field on the department schema, rendered above the applications list — existing 12 departments omit it; (2) the `PersonStanding` icon-map entries. 6 verified references (ref-49 Efrati 2013; ref-50 Schiavo 2020; ref-51 Boussi-Gross 2013; ref-52 Hadanny 2022; ref-53 Walker/Cifu 2014 null military RCT; ref-54 Bennett 2018) — all resolve via CrossRef. Refs 49–50 → stroke app, 51–54 → TBI app; Long COVID + Fibromyalgia apps carry no refs (point to indications). **Contested-TBI framing at full epistemic force in every locale** — positive chronic/paediatric AND null military findings + participation-effect critique, neither softened. Counts auto-update: departments 12→**13**, references 48→**54** (no hardcoded count surface). "Departments without HBOT" page unaffected. Glossary rehab/neurorehab terms appended to el/de/it/es (pending Phase 8). astro check 0/0/0; verify-refs gate exit 0 (53/54 + 1 uhms.org 503 soft-warn; 6 new DOIs resolve); verify-digest unaffected; build **293 routes** (+5: rehab `[slug]` ×5; no new indication routes). Long COVID / Fibromyalgia / Neurology unchanged (parity). No new editorial-integrity catch (the schema `intro` gap was an expected additive need, not a defect); **cumulative catches unchanged at 17.** PR opened; awaiting review.
- **2026-05-29** — **Site search (Pagefind) shipped (PR #16, merge `4baf947`).** Full-text search via the `astro-pagefind` integration (build-time index, client-side WASM, no backend), surfaced as a search icon beside the globe that opens a native modal `<dialog>`. **Zero-JS-on-load preserved (the one sanctioned exception):** the classic `pagefind-ui` bundle (JS/WASM/CSS) is lazy-loaded on first open ONLY — verified in-browser that a content page fetches 0 Pagefind resources and `window.PagefindUI` is undefined until the modal opens. Chose the classic `pagefind-ui` (lazy-mounted via injected `<script>`/`<link>`) over astro-pagefind's v2 component-ui, which loads on page-load and would have broken the rule. Index anchored to `<main data-pagefind-body>` in BaseLayout (nav/footer/globe chrome sit outside it; breadcrumbs + DetailLayout cross-link asides excluded via `data-pagefind-ignore`) — confirmed chrome strings absent from the index. **Locale-scoped** by `<html lang>` (Pagefind per-language indexes) — verified live EN→only `/` results, EL→only `/el/` results, zero cross-locale leak; localised result-count messages render ("10 results for…" / "6 αποτελέσματα για…"). Keyboard: ⌘/Ctrl-K + "/" open ("/" ignored while typing in input/textarea/select/contentEditable), Esc/X/backdrop close, focus moves into the input on open and returns to the trigger on close. Localised `search.*` strings (label/heading/placeholder/clear/load-more/result-count) added to all five locales (additions only; EN/EL/DE/IT/ES parity). New deps are **dev-only**: `astro-pagefind` + `pagefind`; no runtime deps added. astro check 0/0/0; verify-refs 53/54 + verify-digest 23/24 (the two known non-fatal soft-warns); build **293 routes** (unchanged; index emitted to `/pagefind/`, not `/_pagefind/` — astro-pagefind v2 path). **Lighthouse mobile on the warmed Cloudflare preview: Performance 100 / A11y 100 / Best Practices 100; axe-core 4.10.2 on the modal-open state = 0 violations (21 passes).** **Greek tokenisation watch-point (honest note):** Pagefind segments Greek by word but does not stem it — basic/prefix matches work (verified "οξυγόνο"→21, "υπερβαρική οξυγόνο"→6) but inflected variants of a lemma won't all collapse to one stem; known Pagefind limitation, not a regression. Minor non-blocking follow-up: `/404.html` is indexed (it carries `data-pagefind-body`; low content so unlikely to rank — could be excluded with `data-pagefind-ignore` later). No new editorial-integrity catch; **cumulative catches unchanged at 17.**
- **2026-05-29** — **Contact form (two-mode) + 404 search-ignore shipped (PR #17, merge `139703d`).** New `/contact/` page in all five locales with a reason dropdown driving two modes: a structured **research-submission** path (submission type · DOI/link · title · related indication/department · relevance · name · affiliation · role/credentials · email + editorial-independence notice) and a **general-contact** path (name · email · message) for Corrections/Partnership/Media/General. One page, two modes, one inbox (no per-reason label). Server-rendered Astro `<form>` + one lean bundled controller (no React island); hidden-mode controls are `disabled` so they are excluded from native validation + FormData until their mode is active. New Cloudflare Pages Function `functions/api/contact.ts` mirrors `subscribe.ts`: per-IP rate-limit → server-side Turnstile verify → server-side per-mode field validation (trim + length-cap) → one email to a single inbox via **Resend** (dependency-free `fetch`; chosen because the project had no general email sender — subscribe uses Buttondown, a newsletter API — and MailChannels' free Cloudflare route was retired in 2024; `reply_to` = submitter for one-click replies). Footer gains a `/contact/` link in every locale (reuses existing `nav.contact`). `contact.*` i18n keys added to all five locales (additions only; EN/EL/DE/IT/ES parity). **Part C:** `404.astro` marked `data-pagefind-ignore` — `/404.html` now out of the Pagefind index (0 fragments reference it; 297 written vs 298 routes). `.dev.vars` + `.wrangler/` added to `.gitignore`. **Backend tested via `wrangler pages dev` + Cloudflare test Turnstile secret:** valid research + general reach delivery (`provider_error` on the dummy Resend key = `sent` with a real key); missing-field / invalid-type / invalid-reason / invalid-email → 400; empty Turnstile → 403; malformed JSON → 400; 6th req/min/IP → 429. **Frontend (browser, against the real Function via wrangler):** dropdown switches modes exactly (research 9 fields + notice; general 3), client validation + `aria-invalid` + focus-to-first-invalid, success + error states (input never lost on error), editorial notice localised on the research path; axe-core 4.10.2 = 0 violations / 24 passes. astro check 0/0/0; verify-refs 53/54 + verify-digest 23/24 (known non-fatal soft-warns); build **298 routes (+5: `/contact/` × 5 locales)**. **Lighthouse `/contact/` mobile preview: Performance 100 / Accessibility 100 / Best Practices 100.** **Env vars Stamos must set in Cloudflare Pages (Production + Preview) before the form can send:** `TURNSTILE_SECRET_KEY` (secret), `RESEND_API_KEY` (secret), `CONTACT_DEST_EMAIL` (plaintext inbox), `CONTACT_FROM_EMAIL` (plaintext — a verified Resend sender) — plus `hbotscience.org` domain verification in Resend. Until then the form shows the error state on submit (confirmed live on preview: `500 server_misconfigured`). No new editorial-integrity catch; **cumulative catches unchanged at 17.**
